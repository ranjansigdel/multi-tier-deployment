# Use the official Nginx image as a base image
FROM nginx:latest

# Copy custom Nginx config file (if you have one) to override default settings
# COPY nginx.conf /etc/nginx/nginx.conf  # Uncomment if you have a custom config file

# Copy the frontend files (HTML, CSS, JS) into the Nginx web root directory
COPY ./html /usr/share/nginx/html

# Expose port 80 (default HTTP port)
EXPOSE 80
